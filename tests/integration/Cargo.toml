[package]
name    = "integration"
version = "0.1.0"
edition = "2021"

[dependencies]
wdk-safe        = { path = "../../crates/wdk-safe", features = ["test-utils"] }
wdk-safe-macros = { path = "../../crates/wdk-safe-macros" }